#!/usr/bin/env python3

list = [x for x in range(int(input('Insira o valor inicial:', )),int(input('Insira o valor final:', )))]
print(list)

#No enunciado fala que posso salvar o script como 'count.py 3 10' para que esses sejam os valores iniciais e finais, mas como inicialmente o pedido é para que os valores sejam dados pela linha de comando, não salvarei o arquivo com esse nome.
